type Bet_BetAccepted {
  id: ID!
  address: String!
}

type Bet_BetCancelled {
  id: ID!
  address: String!
}

type Bet_BetCreated {
  id: ID!
  address: String!
  maker: String!
  taker: String!
  judge: String!
  asset: String!
  acceptBy: BigInt!
  resolveBy: BigInt!
  makerStake: BigInt!
  takerStake: BigInt!
  description: String!
  createdAt: Int!
}

type Bet_BetResolved {
  id: ID!
  address: String!
  winner: String!
  amount: BigInt!
}

type Bet_Initialized {
  id: ID!
  address: String!
  version: BigInt!
}

type BetFactory_BetCreated {
  id: ID!
  bet: String!
}

type BetFactory_ImplementationUpdated {
  id: ID!
  newImplementation: String!
}

type BetFactory_OwnershipTransferred {
  id: ID!
  previousOwner: String!
  newOwner: String!
}

type BetFactory_PoolConfigured {
  id: ID!
  token: String!
  pool: String!
}

type BetFactory_TreasuryUpdated {
  id: ID!
  newTreasury: String!
}
